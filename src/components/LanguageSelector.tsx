
import React from "react";

// Mock language selector that does nothing since we removed i18n
const LanguageSelector: React.FC = () => {
  return null; // Don't render anything since we're English-only now
};

export default LanguageSelector;
